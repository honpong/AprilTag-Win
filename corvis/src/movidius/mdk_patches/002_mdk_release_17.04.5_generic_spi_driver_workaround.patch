--- mdk_release_17.04.5_general_purpose/mdk/common/drivers/myriad2/socDrivers/leon/rtems/src/OsSpiCommon.c	2017-05-25 16:43:12.527973906 +0300
+++ mdk_release_17.04.5_general_purpose_patched/mdk/common/drivers/myriad2/socDrivers/leon/rtems/src/OsSpiCommon.c	2017-05-25 12:44:31.143653000 +0300
@@ -82,9 +82,13 @@
        rtems_interrupt_enable(level);
     }
 
+    // Workaround for corrupt data being read
+    // when higher priority irq adds delay in this function
+    rtems_interrupt_disable(level);
     rc = HglSpiTxHandler(&handler->hglHndl, &txRemainingSize);
+    rtems_interrupt_enable(level);
     if (rc)
-        txDone(handler);
+    	txDone(handler);
 
     if (txRemainingSize <= 0)
     {
